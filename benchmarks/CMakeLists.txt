# Micro benchmarks — docs/19_TECHNOLOGY_DECISION.md §12.
#
# Built only when TC_BUILD_BENCHMARKS=ON. System-level benchmarking (algorithm
# x scenario x seed x robot count) is a separate runner, added in Phase 15;
# see docs/15_ALGORITHM_BENCHMARK.md.

# Phase 15: add_executable(tc_benchmarks ...)
